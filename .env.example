# Set a private curator PIN in Vercel. Do not commit your real PIN.
ADMIN_PIN=change-this-before-deploying

# SolarPulse currently uses local data/localStorage. This placeholder allows
# the optional database health route to initialize during a hosted build.
DATABASE_URL=postgresql://localhost:5432/placeholder
